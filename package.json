{
  "name": "ens-ideas-workspace",
  "private": true,
  "type": "module",
  "scripts": {
    "build:site": "CLOUDFLARE=1 pnpm --filter ./apps/site run build",
    "deploy:beta": "pnpm run build:site && alchemy deploy --env-file .env --stage beta",
    "deploy:prod": "pnpm run build:site && alchemy deploy --env-file .env --stage prod",
    "run:beta": "alchemy run --env-file .env --stage beta",
    "destroy:beta": "alchemy destroy --env-file .env --stage beta"
  },
  "devDependencies": {
    "alchemy": "0.93.12"
  }
}
